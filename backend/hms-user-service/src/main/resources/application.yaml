spring:
  application:
    name: hms-user-service
  datasource:
    url: jdbc:mysql://localhost:3307/hms_db?useSSL=false
    username: user
    password: user
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
